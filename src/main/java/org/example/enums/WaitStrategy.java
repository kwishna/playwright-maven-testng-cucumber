package org.example.enums;

public class WaitStrategy {
    public static enum LocatorStrategy {
        CLICKABLE, PRESENCE, VISIBLE, HIDDEN, DETTACHED
    }

    public static enum ElementStrategy {
        CLICKABLE, PRESENCE, VISIBLE, HIDDEN, ENABLED, DISABLED
    }

}
